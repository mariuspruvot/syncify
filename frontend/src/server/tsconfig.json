{
  "extends": "../.nuxt/tsconfig.server.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },
    "types": ["node"]
  },
  "include": [
    "nuxt.config.ts",
    "server/**/*.ts", // Make sure server files are included
    "components/**/*.ts",
    "pages/**/*.ts"
  ]
}
